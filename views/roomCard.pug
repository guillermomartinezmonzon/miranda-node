.room-card-container
  .room-card-container__image
  .room-card-container__card
    include ./iconsRoomCard.pug
    .room-card-container__card__title= `${room.name} - ${room.number}`
    .room-card-container__card__text= room.facilities
    .room-card-container__card__book
      .room-card-container__card__book__price= `$${room.price}/night`
      a.room-card-container__card__book__booking(href=`room/${room.id}`) Booking Now
          
